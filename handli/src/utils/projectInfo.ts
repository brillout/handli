export { projectInfo }

const PROJECT_VERSION = '0.0.2'

const projectInfo = {
  projectName: 'Handli',
  projectVersion: PROJECT_VERSION,
  npmPackageName: 'handli',
  githubRepository: 'https://github.com/brillout/handli',
} as const
