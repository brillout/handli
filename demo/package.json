{
  "private": true,
  "scripts": {
    "dev": "vite --port 3000",
    "build": "cd src && parcel build *.html --public-url /handli/",
    "deploy": "pnpm run build && cd .. && git checkout gh-pages && git reset --hard clean-dist && git reset origin/gh-pages && mv live-demo/src/dist/* . && git add -A && git commit -m 'deploy new live demo' && git push && git checkout main"
  },
  "dependencies": {
    "handli": "0.0.2",
    "prismjs": "^1.15.0",
    "react": "16.7.0-alpha.2",
    "react-dom": "16.7.0-alpha.2",
    "react-toastify": "^4.4.3"
  },
  "devDependencies": {
    "vite": "^5.1.6"
  }
}
