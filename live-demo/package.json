{
  "private": true,
  "scripts": {
    "build": "cd client && parcel build *.html",
    "build-dev": "cd client && parcel watch *.html",
    "server": "node ./server/start.js",
    "install-yarn": "npm install --only=dev --no-save",
    "setup": "npm run install-yarn && yarn",
    "setup-dev": "npm run install-yarn && (cd .. && yarn && yarn link) && yarn link handli && yarn"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "express": "^4.16.4",
    "handli": "*",
    "parcel": "^1.10.3",
    "prismjs": "^1.15.0",
    "react": "^16.7.0-alpha.2",
    "react-dom": "^16.7.0-alpha.2"
  },
  "devDependencies": {
    "parcel-plugin-static-files-copy": "^1.2.4",
    "yarn": "1.12.3"
  }
}
